{
  "name": "ai-event-planner-bilingual",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered bilingual event planning web application",
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend && npm run copy:public",
    "copy:public": "rm -rf public && cp -r frontend/dist public",
    "start": "NODE_ENV=production node backend/src/index.js",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
